{
  "scripts": {
    "compile": "tsc -b tsconfig.build.json",
    "fmt": "prettier --write packages/*/src packages/*/bin"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@changesets/cli": "^2.27.8",
    "prettier": "^3.3.3"
  }
}
