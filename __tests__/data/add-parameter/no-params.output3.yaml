paths:
  /objects/{id}:
    get:
      summary: Get an object
      parameters:
        - name: Idempotency-Key
          in: header
          schema:
            type: string
  /collections/{id}:
    get:
      summary: Get a collection
